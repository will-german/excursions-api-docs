get:
  tags:
    - User Management
  summary: Get User By ID
  description: Return a user object with a matching Id.
  operationId: get-user-by-id

  security:
    - BearerToken: []

  parameters:
    - in: path
      name: userID
      schema:
        type: string
        $ref: '../components/schemas/ID.yaml'
      required: true
      description: The ID of the user to get.

  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              user:
                $ref: '../components/schemas/user/User.yaml'
    400:
      $ref: '../components/schemas/response/400.yaml'
    401:
      $ref: '../components/schemas/response/401.yaml'
    500:
      $ref: '../components/schemas/response/500.yaml'