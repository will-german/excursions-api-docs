description: Created

content:
  application/json:
    schema:
      $ref: "../schemas/Response.yaml"

    examples:
      "Unauthorized":
        value:
          $ref: '../../examples/responses/201Created.yaml'