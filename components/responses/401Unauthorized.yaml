description: Unauthorized

content:
  application/json:
    schema:
      $ref: "../schemas/Response.yaml"

    examples:
      "Unauthorized":
        value:
          $ref: '../../examples/responses/401Unauthorized.yaml'