description: Internal Server Error

content:
  application/json:
    schema:
      $ref: "../schemas/Response.yaml"

    examples:
      "Unauthorized":
        value:
          $ref: '../../examples/responses/500InternalServerError.yaml'