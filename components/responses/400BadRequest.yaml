description: Bad Request

content:
  application/json:
    schema:
      $ref: "../schemas/Response.yaml"

    examples:
      "Unauthorized":
        value:
          $ref: '../../examples/responses/400BadRequest.yaml'