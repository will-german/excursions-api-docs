allOf:
  - $ref: '../base/Trip.yaml'

  - type: object
    properties:
      _id:
        $ref: '../ID.yaml'
      host:
        $ref: '../ID.yaml'
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time